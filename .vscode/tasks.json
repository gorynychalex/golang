{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Go Program",
            "type": "shell",
            "command": "go run ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
